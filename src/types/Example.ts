// Used to define the types for the data used in the application. In the types folder we define the data structure for the data from the API.

export interface ExampleType {
  id: number;
  name: string;
}
